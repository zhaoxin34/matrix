---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ecommerce/backend/requirements.txt
  - ecommerce/backend/tests/__init__.py
  - ecommerce/backend/tests/conftest.py
  - ecommerce/backend/tests/test_auth_service.py
autonomous: true
must_haves:
  truths:
    - "Auth service password hashing works correctly"
    - "JWT tokens can be created and decoded"
    - "Auth service authenticate method validates credentials properly"
  artifacts:
    - path: "ecommerce/backend/requirements.txt"
      provides: "pytest and testing dependencies"
    - path: "ecommerce/backend/tests/conftest.py"
      provides: "test fixtures and database mock"
    - path: "ecommerce/backend/tests/test_auth_service.py"
      provides: "auth service unit tests"
  key_links:
    - from: "ecommerce/backend/tests/test_auth_service.py"
      to: "ecommerce/backend/src/app/services/auth_service.py"
      via: "direct import"
---

<objective>
Set up pytest testing infrastructure and write unit tests for auth_service.
</objective>

<execution_context>
@/Volumes/data/working/ai/matrix/ecommerce/backend/src/app/services/auth_service.py
@/Volumes/data/working/ai/matrix/ecommerce/backend/src/app/schemas/auth.py
@/Volumes/data/working/ai/matrix/ecommerce/backend/src/app/config.py
@/Volumes/data/working/ai/matrix/ecommerce/backend/src/app/models/user.py
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add pytest dependencies to requirements.txt</name>
  <files>ecommerce/backend/requirements.txt</files>
  <action>
Add the following testing dependencies to requirements.txt. Append these new lines after the existing content (which ends with "# Utilities\npython-multipart>=0.0.6"):

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.26.0
</action>
  <verify>
<automated>grep -q "pytest>=8.0.0" /Volumes/data/working/ai/matrix/ecommerce/backend/requirements.txt && echo "PASS"</automated>
</verify>
  <done>pytest and testing deps added to requirements.txt</done>
</task>

<task type="auto">
  <name>Task 2: Create test configuration with fixtures</name>
  <files>ecommerce/backend/tests/conftest.py</files>
  <action>
Create pytest configuration file at `ecommerce/backend/tests/conftest.py` with the following content:

```python
"""Test configuration and fixtures."""

import os
import sys
from datetime import datetime, timedelta
from unittest.mock import MagicMock, patch

import pytest

# Add src to path for imports
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

# Set test environment before importing app modules
os.environ["SECRET_KEY"] = "test-secret-key-for-testing-only"
os.environ["DATABASE_URL"] = "sqlite:///:memory:"
os.environ["ACCESS_TOKEN_EXPIRE_MINUTES"] = "30"
os.environ["REFRESH_TOKEN_EXPIRE_DAYS"] = "7"


@pytest.fixture
def mock_db():
    """Create a mock database session."""
    db = MagicMock()
    return db


@pytest.fixture
def sample_user():
    """Create a sample user mock with hashed password."""
    user = MagicMock()
    user.id = 1
    user.username = "testuser"
    user.phone = "13800138000"
    user.email = "test@example.com"
    user.hashed_password = "$2b$12$LQv3c1yqBWVHxkd0LHAkCOYz6TtxMQJqhN8/X4.F1mVrS1I/FgP8K"  # "Test1234"
    user.failed_login_attempts = 0
    user.locked_until = None
    user.sms_code = None
    user.sms_code_expires_at = None
    user.password_history = None
    return user
```
</action>
  <verify>
<automated>python -c "import sys; sys.path.insert(0, 'tests'); import conftest; print('PASS')"</automated>
</verify>
  <done>conftest.py created with mock_db and sample_user fixtures</done>
</task>

<task type="auto">
  <name>Task 3: Write auth service unit tests</name>
  <files>ecommerce/backend/tests/test_auth_service.py</files>
  <action>
Create test file at `ecommerce/backend/tests/test_auth_service.py` with comprehensive tests for the auth service.

Key test cases to implement:

1. **test_hash_password** - verify password hashing works and produces different hashes
2. **test_verify_password** - verify correct password matches hash
3. **test_create_access_token** - verify JWT access token is created with correct structure
4. **test_create_refresh_token** - verify JWT refresh token is created
5. **test_decode_token** - verify valid tokens decode correctly, invalid return None
6. **test_authenticate_success** - mock db + user, verify tokens returned
7. **test_authenticate_wrong_password** - verify ValueError raised
8. **test_authenticate_user_not_found** - verify ValueError raised
9. **test_authenticate_account_locked** - verify lock check works
10. **test_authenticate_increments_failed_attempts** - verify failed attempts tracked
11. **test_refresh_access_token** - verify refresh token exchange works
12. **test_reset_password** - verify password reset with valid code
13. **test_verify_sms_code_valid** - verify valid SMS code verification
14. **test_verify_sms_code_invalid** - verify invalid code returns False

Use the mock_db and sample_user fixtures. Mock at the boundary (AuthService methods that interact with DB) rather than mocking internal implementation.
</action>
  <verify>
<automated>cd /Volumes/data/working/ai/matrix/ecommerce/backend && pip install -q pytest pytest-asyncio httpx 2>/dev/null && python -m pytest tests/test_auth_service.py -v --tb=short 2>&1 | tail -40</automated>
</verify>
  <done>30+ unit tests for auth service covering password hashing, JWT tokens, and authentication logic</done>
</task>

</tasks>

<verification>
cd /Volumes/data/working/ai/matrix/ecommerce/backend && pip install -q pytest pytest-asyncio httpx 2>/dev/null && python -m pytest tests/test_auth_service.py -v --tb=short
</verification>

<success_criteria>
- pytest>=8.0.0 added to requirements.txt
- conftest.py with mock_db and sample_user fixtures
- 30+ unit tests covering: password hashing, JWT creation/decoding, AuthService methods
- All tests pass with `pytest tests/test_auth_service.py -v`
</success_criteria>

<output>
After completion, create `.planning/quick/260416-va7-add-tests/260416-va7-SUMMARY.md`
</output>
