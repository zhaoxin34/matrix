---
phase: "06-shopping-cart"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  backend:
    - "ecommerce/backend/src/app/models/cart.py"
    - "ecommerce/backend/src/app/services/cart_service.py"
    - "ecommerce/backend/src/app/repositories/cart_repo.py"
    - "ecommerce/backend/src/app/api/v1/cart.py"
    - "ecommerce/backend/src/app/dependencies.py"
    - "ecommerce/backend/src/app/main.py"
  frontend: []
  planning:
    - ".planning/phases/06-shopping-cart/06-01-PLAN.md"
autonomous: true
requirements_addressed: ["CART-01", "CART-02", "CART-03"]
must_haves:
  truths:
    - "Guest users can add items to cart via session_id cookie"
    - "Authenticated users' carts are linked to their user_id from JWT"
    - "Cart items store selected SKU variant as JSON"
    - "Cart API endpoints resolve user identity from JWT or session_id cookie"
  artifacts:
    - path: "ecommerce/backend/src/app/models/cart.py"
      provides: "CartItem with session_id and sku_variant fields"
      contains: "session_id.*mapped_column"
    - path: "ecommerce/backend/src/app/api/v1/cart.py"
      provides: "Cart endpoints using auth dependency instead of hardcoded user_id=1"
      exports: ["GET /cart/items", "POST /cart/items", "PUT /cart/items/{id}", "DELETE /cart/items/{id}"]
    - path: "ecommerce/backend/src/app/services/cart_service.py"
      provides: "Cart business logic with session_id support and merge functionality"
      exports: ["get_by_user", "get_by_session", "merge_guest_cart"]
  key_links:
    - from: "ecommerce/backend/src/app/api/v1/cart.py"
      to: "ecommerce/backend/src/app/services/cart_service.py"
      via: "CartService instance"
      pattern: "CartService\\(db\\)"
    - from: "ecommerce/backend/src/app/models/cart.py"
      to: "ecommerce/backend/src/app/models/user.py"
      via: "user_id foreign key"
      pattern: "ForeignKey\\(\\\"users.id\\\"\\)"
---

<objective>
Implement backend cart infrastructure supporting both guest users (session_id) and authenticated users (user_id from JWT). Add sku_variant storage to CartItem model. Replace hardcoded user_id=1 with proper auth/session resolution.

Purpose: Foundation for backend-synced cart per D-01, guest cart support per D-02, SKU variant storage per D-05.
Output: Updated CartItem model with session_id and sku_variant fields, cart API routes with proper auth/session resolution, cart service with merge logic.
</objective>

<execution_context>
@/Volumes/data/working/ai/matrix/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/data/working/ai/matrix/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@ecommerce/backend/src/app/models/cart.py
@ecommerce/backend/src/app/services/cart_service.py
@ecommerce/backend/src/app/repositories/cart_repo.py
@ecommerce/backend/src/app/api/v1/cart.py
@ecommerce/backend/src/app/dependencies.py
@ecommerce/backend/src/app/models/user.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CartItem model with session_id and sku_variant fields</name>
  <files>ecommerce/backend/src/app/models/cart.py</files>
  <action>
    Read the file first.

    Add TWO new fields to the CartItem model:
    1. `session_id: Mapped[str | None]` - mapped_column(String(64), nullable=True, index=True) for guest cart identification
    2. `sku_variant: Mapped[str | None]` - mapped_column(Text, nullable=True) to store selected SKU options as JSON string (e.g., '{"color": "red", "size": "M"}')

    Keep existing `user_id` field as nullable (ForeignKey still non-nullable but individual rows can have either user_id OR session_id).

    Ensure relationships remain intact: `user` relationship and `product` relationship.
  </action>
  <verify>
    <automated>grep -n "session_id\|sku_variant" /Volumes/data/working/ai/matrix/ecommerce/backend/src/app/models/cart.py</automated>
  </verify>
  <done>CartItem model has session_id (String, nullable, indexed) and sku_variant (Text, nullable) fields. Existing user_id and product_id fields unchanged. Relationships preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Update cart repository with session-based queries</name>
  <files>ecommerce/backend/src/app/repositories/cart_repo.py</files>
  <action>
    Read the file first.

    Add THREE new methods to CartRepository class:

    1. `get_by_session(self, session_id: str) -> list[CartItem]`
       - Query cart_items by session_id
       - Return all items for that session

    2. `get_by_session_and_product(self, session_id: str, product_id: int) -> CartItem | None`
       - Query by session_id AND product_id
       - Used for checking if guest already has item in cart

    3. `create_for_session(self, session_id: str, cart_data: CartItemCreate) -> CartItem`
       - Create new cart item with session_id instead of user_id
       - Similar to existing create() but sets session_id

    4. `delete_by_session(self, session_id: str) -> None`
       - Delete all cart items for a session
       - Used when merging guest cart into user cart (delete guest items after merge)

    5. `update_by_session_and_product(self, session_id: str, product_id: int, quantity: int) -> CartItem | None`
       - Update quantity for existing guest cart item
       - Used when guest adds same product again

    Keep existing methods unchanged (they work with user_id).
  </action>
  <verify>
    <automated>grep -n "def get_by_session\|def create_for_session\|def delete_by_session" /Volumes/data/working/ai/matrix/ecommerce/backend/src/app/repositories/cart_repo.py</automated>
  </verify>
  <done>CartRepository has get_by_session(), create_for_session(), delete_by_session(), get_by_session_and_product(), update_by_session_and_product() methods. Existing methods unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Update cart service with session support and merge logic</name>
  <files>ecommerce/backend/src/app/services/cart_service.py</files>
  <action>
    Read the file first.

    Modify CartService class to support both user_id and session_id:

    1. Update __init__ to accept optional session_id parameter:
       ```python
       def __init__(self, db: Session, session_id: str | None = None):
           self.db = db
           self.repo = CartRepository(db)
           self.session_id = session_id
       ```

    2. Update get_by_user() and get_multi() to remain for authenticated users.

    3. Add new methods:
       - `get_for_identity(user_id: int | None, session_id: str | None) -> list[CartItem]` - Returns cart items based on who is making the request
       - `get_cart_total_for_identity(user_id: int | None, session_id: str | None) -> float` - Calculate total for either user or session
       - `create_for_identity(user_id: int | None, session_id: str | None, cart_data: CartItemCreate) -> CartItem` - Create cart item for either identity
       - `merge_guest_cart(user_id: int, guest_session_id: str) -> int` - Merge guest cart into user cart, returns count of items merged. Logic: for each guest cart item, if user already has that product, add quantities; otherwise, update guest item's user_id to user_id and clear session_id. Then delete guest cart.
       - `count_for_identity(user_id: int | None, session_id: str | None) -> int`

    4. The existing `create()` method should detect if session_id is set vs user_id and route accordingly.

    5. Add `sku_variant: str | None` parameter to create() and update() - store as JSON string.
  </action>
  <verify>
    <automated>grep -n "def merge_guest_cart\|def get_for_identity\|def get_cart_total_for_identity" /Volumes/data/working/ai/matrix/ecommerce/backend/src/app/services/cart_service.py</automated>
  </verify>
  <done>CartService has merge_guest_cart(), get_for_identity(), get_cart_total_for_identity(), count_for_identity() methods. create() and update() accept sku_variant parameter. Service routes between user_id and session_id based on which is provided.</done>
</task>

<task type="auto">
  <name>Task 4: Update cart API routes with auth/session resolution</name>
  <files>ecommerce/backend/src/app/api/v1/cart.py</files>
  <action>
    Read the file first.

    Rewrite cart API routes to use proper auth/session resolution:

    1. Add dependency import for get_current_user and Cookie header:
       ```python
       from fastapi import Cookie, Depends, HTTPException, status
       from app.dependencies import get_current_user
       from app.models.user import User
       ```

    2. Create a helper function to resolve identity:
       ```python
       def get_cart_identity(
           user: User | None = Depends(get_current_user),
           session_id: str | None = Cookie(default=None)
       ) -> tuple[int | None, str | None]:
           return (user.id if user else None, session_id)
       ```

    3. Update each endpoint:
       - `GET /items` - Use get_cart_identity, call service.get_for_identity(user_id, session_id), calculate total with get_cart_total_for_identity()
       - `POST /items` - Use get_cart_identity, accept CartItemCreate with new fields (product_id, quantity, sku_variant), call service.create_for_identity()
       - `PUT /{cart_item_id}` - Keep as is but user can only update their own items (verify user_id or session_id matches)
       - `DELETE /{cart_item_id}` - Keep as is but user can only delete their own items
       - `DELETE /items` (clear) - Clear cart for the current identity

    4. Update CartItemCreate schema in action to include optional sku_variant field.

    5. IMPORTANT: The Cookie parameter requires a default value. Use:
       `session_id: str | None = Cookie(default=None)`

    6. The frontend passes session_id via cookie named "cart_session_id". Ensure Cookie name matches.

    Remove the hardcoded `user_id: int = 1` default from all endpoints.
  </action>
  <verify>
    <automated>grep -n "user_id: int = 1\|def get_cart_identity\|@router" /Volumes/data/working/ai/matrix/ecommerce/backend/src/app/api/v1/cart.py</automated>
  </verify>
  <done>Cart API routes use get_cart_identity() to resolve user_id from JWT or session_id from cookie. No hardcoded user_id=1. All endpoints work for both authenticated users and guest sessions. Stock is validated when adding to cart.</done>
</task>

<task type="auto">
  <name>Task 5: Update cart schemas to include sku_variant</name>
  <files>ecommerce/backend/src/app/schemas/cart.py</files>
  <action>
    Read the file first.

    Update the cart schemas:

    1. In CartItemBase, add:
       ```python
       sku_variant: dict | None = None
       ```

    2. In CartItemCreate (BaseCreate), add:
       ```python
       sku_variant: dict | None = None
       ```

    3. In CartItemResponse, ensure it includes sku_variant.

    4. Update CartItemUpdate if needed to allow updating sku_variant (optional, can be used if user changes variant).

    The BaseCreate and BaseUpdate schemas come from app.schemas.common - check if they support extra fields. If not, override to add sku_variant.
  </action>
  <verify>
    <automated>grep -n "sku_variant" /Volumes/data/working/ai/matrix/ecommerce/backend/src/app/schemas/cart.py</automated>
  </verify>
  <done>CartItemBase, CartItemCreate, and CartItemResponse schemas all include sku_variant field. CartItemUpdate includes sku_variant for variant changes.</done>
</task>

<task type="auto">
  <name>Task 6: Add cart merge endpoint for login flow</name>
  <files>ecommerce/backend/src/app/api/v1/auth.py</files>
  <action>
    Read the file first.

    In auth.py, after the login endpoint (`POST /auth/login`), add cart merge logic:

    1. The login endpoint already returns TokenResponse with access_token and refresh_token.

    2. After successful authentication (after `self._create_tokens_for_user(user)`), if a guest_session_id cookie is present, call CartService.merge_guest_cart(user.id, guest_session_id).

    3. Add import:
       ```python
       from app.services.cart_service import CartService
       ```

    4. Modify the authenticate() method or the endpoint that calls it to accept optional guest_session_id and perform merge.

    5. The login endpoint signature becomes:
       ```python
       def login(
           login_data: UserLogin,
           guest_session_id: str | None = Cookie(default=None),
           db: Session = Depends(get_db)
       ) -> TokenResponse:
       ```

    6. After getting token response, if guest_session_id exists:
       ```python
       cart_service = CartService(db)
       cart_service.merge_guest_cart(user.id, guest_session_id)
       ```

    Note: Only merge if guest_session_id is valid non-empty string.
  </action>
  <verify>
  <automated>grep -n "merge_guest_cart\|guest_session_id" /Volumes/data/working/ai/matrix/ecommerce/backend/src/app/api/v1/auth.py</automated>
  </verify>
  <done>Login endpoint merges guest cart into user cart when guest_session_id cookie is present. Cart merge happens after successful authentication, before returning tokens.</done>
</task>

</tasks>

<verification>
## Backend Verification

After implementing Wave 1, verify:

1. CartItem model has session_id and sku_variant columns in database:
   ```bash
   cd /Volumes/data/working/ai/matrix/ecommerce/backend && python -c "from app.models.cart import CartItem; print(CartItem.__table__.columns.keys())"
   ```

2. Cart API endpoints resolve identity correctly:
   - Guest request (no JWT, with session_id cookie) returns that session's cart
   - Authenticated request (with valid JWT) returns user's cart

3. Cart merge works on login (tested via integration test or manual verification)
</verification>

<success_criteria>
- CartItem model has session_id and sku_variant fields
- Cart API no longer uses hardcoded user_id=1
- Cart API accepts both JWT auth and session_id cookie for guest users
- Cart merge happens on login when guest_session_id cookie present
- All existing cart operations (add, update, delete, clear) work with new identity resolution
</success_criteria>

<output>
After completion, create `.planning/phases/06-shopping-cart/06-01-SUMMARY.md`
</output>
