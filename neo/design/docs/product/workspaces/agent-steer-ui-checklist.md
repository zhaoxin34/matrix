---
id: agent-steer-ui-checklist
title: Agent Steer UI 需求清单
sidebar_position: 22
author: Claude
created: 2026-06-09
updated: 2026-06-09
version: 1.0.0
tags: [Agent, Steer, UI, Checklist]
---

> 基于产品设计文档 `design/docs/product/workspaces/agent-steer.md` 提取
> 状态: UI 原型已完成 ✅

---

## 1. 模式切换 (Mode Switching)

### 1.1 模式选择器

| 元素 | 类型 | 说明 | 开发状态 |
|------|------|------|----------|
| 学习模式按钮 | Button | 显示"📚 学习" | ✅ 已实现 |
| 指导模式按钮 | Button | 显示"🎥 指导" | ✅ 已实现 |
| 主动模式按钮 | Button | 显示"🚀 主动" | ✅ 已实现（占位） |

### 1.2 交互行为

| 行为 | 触发条件 | 预期结果 | 开发状态 |
|------|----------|----------|----------|
| 点击模式按钮 | 用户点击任意模式 | 切换到对应模式，更新状态 | ✅ 已实现 |
| 模式切换命令 | 点击后 | 发送 `SET_MODE` 命令到 Content Script | ⚠️ 待集成（UI 已预留） |
| 状态同步 | 模式切换后 | Content Script 返回 `STATE_UPDATE` 同步状态 | ⚠️ 待集成（状态机已实现） |

---

## 2. 录制控制 (Recording Control)

### 2.1 录制按钮

| 按钮 | 状态 | 图标 | 说明 | 开发状态 |
|------|------|------|------|----------|
| 开始录制 | Idle 状态显示 | ▶ + "开始" | 点击后开始录制 | ✅ 已实现 |
| 暂停录制 | Recording 状态显示 | ⏸ + "暂停" | 点击后暂停录制 | ✅ 已实现 |
| 恢复录制 | Paused 状态显示 | ▶ + "恢复" | 点击后恢复录制 | ✅ 已实现 |
| 停止录制 | Recording/Paused 状态显示 | ⏹ + "停止" | 点击后停止录制并保存 | ✅ 已实现 |

### 2.2 录制时长显示

| 元素 | 格式 | 说明 | 开发状态 |
|------|------|------|----------|
| 录制时长 | HH:MM:SS | 实时更新，如 "00:15:32" | ✅ 已实现 |
| 事件计数 | 数字 | 显示录制的事件数量，如 "事件: 128" | ✅ 已实现（模拟数据） |

### 2.3 录制指示器 (Overlay)

| 元素 | 位置 | 说明 | 开发状态 |
|------|------|------|----------|
| Recording 文字 | 录制控制区域下方 | "正在录制..." + 脉冲动画 | ✅ 已实现 |
| 脉冲动画 | 状态点周围 | 绿色脉冲环告知正在录制 | ✅ 已实现 |

---

## 3. 状态显示 (Status Display)

### 3.1 状态类型

| 状态 | 圆点颜色 | 文字显示 | 说明 | 开发状态 |
|------|----------|----------|------|----------|
| Idle | 灰色 (#6b7280) | "空闲" | 未录制 | ✅ 已实现 |
| Recording | 绿色 (#22c55e) + 脉冲 | "录制中" | 正在录制 | ✅ 已实现 |
| Paused | 黄色 (#eab308) | "已暂停" | 录制暂停 | ✅ 已实现 |
| Playing | 蓝色 (#3b82f6) | "回放中" | 正在回放 | ✅ 已实现 |

### 3.2 状态指示器样式

```
┌─────────────────────────────────────┐
│  ● 00:15:32              ⏸  ⏹     │
│  事件: 128                          │
└─────────────────────────────────────┘
```
✅ 已实现，与设计一致

---

## 4. 回放控制 (Playback Control)

### 4.1 rrweb Player

| 元素 | 说明 | 开发状态 |
|------|------|----------|
| Player 容器 | 嵌入 rrweb 回放组件（模拟） | ✅ 已实现（占位容器） |
| 进度条 | 显示播放进度 | ✅ 已实现 |
| 时间显示 | "00:05:32 / 00:10:00" | ✅ 已实现 |
| 操作描述 | 显示当前操作步骤 | ✅ 已实现（模拟数据） |

### 4.2 回放交互

| 元素 | 功能 | 开发状态 |
|------|------|----------|
| 播放/暂停按钮 | 控制回放 | ✅ 已实现 |
| 进度条可拖拽 | 跳转到指定位置 | ✅ 已实现 |
| 停止按钮 | 停止回放 | ✅ 已实现 |
| 重播按钮 | 从头开始回放 | ✅ 已实现 |
| 速度控制 | 1x / 1.5x / 2x 等 | ❌ 未实现（P2，待定） |

---

## 5. UI 布局结构

### 5.1 整体布局

```
┌─────────────────────────────────────┐
│  🔧 Agent Steer                      │
├─────────────────────────────────────┤
│                                     │
│  [模式选择按钮区域]                   │
│  📚 学习  |  🎥 指导  |  🚀 主动    │
│                                     │
│  ─────────────────────────────────  │
│                                     │
│  [录制/回放控制区域]                  │
│                                     │
└─────────────────────────────────────┘
```
✅ 已实现，与设计一致

### 5.2 布局原则

- ✅ 采用卡片式布局，模块化展示
- ✅ 模式切换在最上方
- ✅ 录制控制在中间
- ✅ 状态显示集成在录制/回放区域
- ✅ 回放控制在指导模式下替换录制控制

---

## 6. 待确认问题

| # | 问题 | 优先级 | 状态 | 说明 |
|---|------|--------|------|------|
| 1 | 是否需要显示录制指示器（Overlay）？如果需要，样式是什么？ | P1 | ✅ 已确认 | 在控制区域内显示脉冲动画 |
| 2 | 回放控制中是否需要速度切换功能？ | P2 | ⚠️ 待定 | 基础回放已完成，可后续添加 |
| 3 | 主动模式的任务列表 UI 是否在此阶段开发？ | P1 | ⚠️ 待确认 | 当前仅显示占位信息 |
| 4 | 遮罩层（指导模式）的样式和位置？ | P1 | ❌ 未实现 | 后续与 rrweb 集成时处理 |
| 5 | UI 的主题色/风格偏好？ | P2 | ✅ 已确认 | 复用 Neo 主题 |

---

## 7. 开发确认清单

### 模式切换
- [x] 三种模式按钮布局确认 ✅
- [x] 模式切换动画/过渡效果确认 ✅（状态驱动）
- [x] 当前模式高亮样式确认 ✅（使用 default variant）

### 录制控制
- [x] 开始/暂停/停止按钮图标确认 ✅
- [x] 录制时长显示格式确认 ✅（HH:MM:SS）
- [x] 事件计数显示位置确认 ✅
- [x] 按钮在不同状态下的可用性确认 ✅

### 状态显示
- [x] 四种状态的圆点颜色确认 ✅
- [x] 状态文字描述确认 ✅
- [x] 录制中的脉冲动画确认 ✅

### 回放控制
- [x] rrweb Player 容器尺寸确认 ✅
- [x] 进度条样式确认 ✅
- [x] 操作描述文字样式确认 ✅
- [x] 播放控制按钮确认 ✅

---

## 8. 技术实现总结

### 已完成
- ✅ 页面路由：`/workspace/[workspace_code]/agent-steer`
- ✅ 组件库：AgentSteer, ModeSelector, RecordingControls, PlaybackControls, StatusDisplay
- ✅ 状态管理：useAgentSteer hook
- ✅ 类型定义：TypeScript 类型完整
- ✅ 样式：复用 Neo 主题，Tailwind CSS
- ✅ 构建验证：通过类型检查、Lint、构建

### 待集成
- ⚠️ rrweb Player 真实组件
- ⚠️ Content Script 消息通信
- ⚠️ IndexedDB 数据持久化
- ⚠️ 遮罩层实现

### 文件清单

```
frontend/app/(main)/workspace/[workspace_code]/agent-steer/
├── page.tsx                           # 页面入口
├── components/
│   ├── index.ts                        # 组件导出
│   ├── AgentSteer.tsx                  # 主容器
│   ├── ModeSelector.tsx                # 模式选择器
│   ├── RecordingControls.tsx           # 录制控制
│   ├── PlaybackControls.tsx            # 回放控制
│   └── StatusDisplay.tsx               # 状态显示
├── hooks/
│   └── useAgentSteer.ts                # 状态管理
└── types/
    └── index.ts                        # 类型定义
```

---

## 下一步建议

1. **查看 UI 原型**：启动前端 `make dev`，访问 `/workspace/test/agent-steer`
2. **验证交互逻辑**：测试模式切换、录制控制、回放控制
3. **确认遮罩层需求**：明确指导模式的遮罩层样式
4. **确定主动模式范围**：是否在此阶段实现任务列表

---

*文档由 AI 辅助生成，需人工审核确认*