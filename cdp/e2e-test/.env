# CDP E2E Test Configuration

# Base URL for CDP application
PLAYWRIGHT_BASE_URL=http://localhost:3002

# Test options
HEADED=false
TIMEOUT=10000

# Database (for backup/restore during tests)
DB_USER=root
DB_PASSWORD=root
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=cdp
